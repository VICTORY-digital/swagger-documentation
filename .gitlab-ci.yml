stages:
  - test
  - publish

#code_quality:
#  stage: test
#  allow_failure: true
#  before_script:
#    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
#  script:
#    - docker run --rm --user $UID --volume `pwd`:/var/www/project source.ananas-web.ru:4437/internal/php:8.0.2 composer install --no-progress
#    - docker run --rm --user $UID --volume `pwd`:/var/www/project source.ananas-web.ru:4437/internal/php:8.0.2 php ./vendor/bin/psalm --no-cache --no-diff --threads=4 --report=./codeclimate.json
#  after_script:
#    - docker run --rm --user $UID --volume `pwd`:/build haynes/jacoco2cobertura:1.0.4 sed -i 's!"[a-z\\/]*\/src!"src!g' /build/codeclimate.json
#  artifacts:
#    expose_as: 'Code Quality Report'
#    paths: [ codeclimate.json ]
#    reports:
#      codequality: codeclimate.json
#  dependencies: [ ]
#  rules:
#    - if: '$CODE_QUALITY_DISABLED'
#      when: never
#    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH'

build:
 stage: test
 script:
   - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
   - docker run --rm --user $UID --volume `pwd`:/var/www/project source.ananas-web.ru:4437/internal/php:8.2.14 composer install --no-progress

publish:
  stage: publish
  variables:
    URL: "$CI_SERVER_PROTOCOL://$CI_SERVER_HOST:$CI_SERVER_PORT/api/v4/projects/$CI_PROJECT_ID/packages/composer?job_token=$CI_JOB_TOKEN"
  script:
    - version=$([[ -z "$CI_COMMIT_TAG" ]] && echo "branch=$CI_COMMIT_REF_NAME" || echo "tag=$CI_COMMIT_TAG")
    - insecure=$([ "$CI_SERVER_PROTOCOL" = "http" ] && echo "--insecure" || echo "")
    - response=$(curl -s -w "\n%{http_code}" $insecure --data $version $URL)
    - code=$(echo "$response" | tail -n 1)
    - body=$(echo "$response" | head -n 1)
    # Output state information
    - if [ $code -eq 201 ]; then
      echo "Package created - Code $code - $body";
      else
      echo "Could not create package - Code $code - $body";
      exit 1;
      fi
  rules:
    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH == "main"'
